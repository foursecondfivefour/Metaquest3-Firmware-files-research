# ДОКЛАД ОБ АНАЛИЗЕ БИНАРНИКА `featenabler.img`

## Часть 3: Детальный разбор функций, покрытых обзорно в частях 1-2 (~86 функций)

---

## 1. НАЗНАЧЕНИЕ ЧАСТИ 3

В частях 1 и 2 был дан **обзорный** разбор всех 208 функций TA. Однако часть из них была представлена только строкой таблицы (без тела) или с прочерком вместо размера. Настоящая часть закрывает этот пробел: здесь детально разобраны **~86 функций**, которые ранее были описаны поверхностно.

| Что разобрано | Кол-во |
|---------------|--------|
| Импорт-обёртки QSEE (0x0-0x180) | 23 + точка входа |
| Jump-трамплины `jump_x*` | 7 |
| DisplayCore (функции с прочерком) | 7 |
| GPParams (вспомогательные) | 14 |
| ObjectList | 6 |
| Прочее (`j_qsee_err_fatal`, `__funcs_on_exit_wrapper`, `nullsub_2/3`, `__stack_chk_guard_ref`) | 5 |
| libc (`scalbn`, `strlcpy`, `memchr`, `memset`, `strlen`, `strchr`) | 6 |
| CommonUtils (кодирование) | 4 |
| Резерв/смежные утилиты | ~14 |

**Важно:** в текущей IDB ровно **208 функций**, 0 `sub_*`, 0 unnamed. Все 208 уже распределены между частями 1-2 по признаку «первая сотня / остальные». Часть 3 не вводит новых функций — она углубляет разбор уже существующих.

---

## 2. ИМПОРТ-ОБЁРКИ QSEE (0x0-0x180)

Это 24 функции по 16 байт каждая, расположенные в самом начале кода. Это **не** реализации — это трамплины к импортируемым функциям QSEE через GOT (Global Offset Table).

### 2.1. Единый шаблон

Все обёртки построены по одному шаблону (проверено дизассемблером на `qsee_log`):

```
ADRP  X16, off_D0xx        ; загрузить страницу GOT-записи
LDR   X17, [X16, #off]     ; прочитать указатель из GOT -> X17
ADD   X16, X16, #off       ; X16 = адрес GOT-записи
BR    X17                  ; переход на реальную функцию
```

Это классический ARM64-паттерн вызова внешней функции через таблицу смещений. IDA показывает комментарий `; __imp_<name>` — прямую привязку к элементу таблицы импорта в сегменте `extern`.

### 2.2. Таблица обёрток

| Адрес | Имя | Целевой импорт | Назначение |
|-------|-----|----------------|------------|
| 0x0 | `__module_entry_thunk` | `qword_D010` | точка входа ELF-образа |
| 0x20 | `qsee_log` | `__imp_qsee_log` @ 0x11338 | логирование (134 xref) |
| 0x30 | `qsee_malloc` | `__imp_qsee_malloc` | аллокация |
| 0x40 | `qsee_free` | `__imp_qsee_free` | освобождение |
| 0x50 | `qsee_err_fatal` | `__imp_qsee_err_fatal` | фатальная ошибка |
| 0x60 | `__funcs_on_exit` | `__imp___funcs_on_exit` | деструкторы при выходе |
| 0x70 | `cmnlib_init` | `__imp_cmnlib_init` | init crypto-lib |
| 0x80 | `GPAppLib_init` | `__imp_GPAppLib_init` | init GP App Library |
| 0x90 | `GPAppLib_appInit` | `__imp_GPAppLib_appInit` | app init |
| 0xa0 | `GPAppLib_appShutdown` | `__imp_GPAppLib_appShutdown` | app shutdown |
| 0xb0 | `cmnlib_release` | `__imp_cmnlib_release` | релиз crypto-lib |
| 0xc0 | `GPAppLib_handleRequest` | `__imp_GPAppLib_handleRequest` | обработка запроса |
| 0xd0 | `CApp_openSession` | `__imp_CApp_openSession` | открытие сессии |
| 0xe0 | `qsee_open` | `__imp_qsee_open` | открытие QSEE-объекта |
| 0xf0 | `qsee_is_sw_fuse_blown` | `__imp_qsee_is_sw_fuse_blown` | проверка fuse |
| 0x100 | `qsee_get_secure_state` | `__imp_qsee_get_secure_state` | чтение secure state |
| 0x110 | `qsee_stor_write_sectors` | `__imp_qsee_stor_write_sectors` | запись секторов |
| 0x120 | `qsee_stor_device_init` | `__imp_qsee_stor_device_init` | init storage device |
| 0x130 | `qsee_stor_device_get_info` | `__imp_qsee_stor_device_get_info` | инфо устройства |
| 0x140 | `qsee_stor_read_sectors` | `__imp_qsee_stor_read_sectors` | чтение секторов |
| 0x150 | `qsee_stor_open_partition` | `__imp_qsee_stor_open_partition` | открытие партиции |
| 0x160 | `qsee_stor_add_partition` | `__imp_qsee_stor_add_partition` | добавление партиции |
| 0x170 | `qsee_stor_client_get_info` | `__imp_qsee_stor_client_get_info` | инфо клиента |
| 0x180 | `qsee_stor_remove_client` | `__imp_qsee_stor_remove_client` | удаление клиента |

### 2.3. Особый случай: `__module_entry_thunk` @ 0x0

Отличается от остальных: вместо GOT-записи в `off_D0xx` использует `qword_D010` — это ячейка среды исполнения, куда загрузчик TEE кладёт адрес реальной точки входа при инициализации образа. Дополнительно пролог сохраняет регистры (`STP X16, X30, [SP, #-0x10]!`), т.к. это самая первая функция, вызываемая загрузчиком.

---

## 3. JUMP-ТРАМПЛИНЫ РЕГИСТРОВ (0x630-0x690)

Семь функций по 8 байт — простейшие непрямые диспетчеры:

```
MOV  Xn, X0     ; скопировать аргумент в целевой регистр
BR   Xn         ; безусловный переход
```

| Адрес | Имя | Целевой регистр | xref |
|-------|-----|-----------------|------|
| 0x630 | `jump_x8` | X8 | 26 |
| 0x640 | `jump_x9` | X9 | 10 |
| 0x650 | `jump_x10` | X10 | 3 |
| 0x660 | `jump_x11` | X11 | - |
| 0x670 | `jump_x20` | X20 | - |
| 0x680 | `jump_x21` | X21 | - |
| 0x690 | `jump_x23` | X23 | - |

**Роль:** это «регистровые вентили». Когда вызывающий код держит адрес цели в конкретном регистре (например, вернул его из предыдущего вызова), но ему нужно выполнить переход по другому регистру ABI — он передаёт управление через такой трамплин. `jump_x8` — самый горячий (26 xref), т.к. X8 в ARM64 — регистр косвенного возврата значения/адреса.

---

## 4. DISPLAYCORE - ДОПОЛНЕНИЕ (функции, помеченные «-» в части 1)

В части 1 семь функций DisplayCore были даны с прочерком. Здесь — их реальные размеры и назначение.

| Адрес | Имя | Размер | Роль |
|-------|-----|--------|------|
| 0x1f18 | `DisplayCore_GetFeatureList` | 464 | получить список фич дисплея |
| 0x20e8 | `DisplayCore_EncodeMetaData` | 8 | заглушка метаданных |
| 0x20f8 | `DisplayCore_Close` | 124 | закрыть дескриптор DisplayCore |
| 0x217c | `DisplayCore_ConfigureSwFuse` | 368 | сконфигурировать SW-fuse |
| 0x22ec | `DisplayCore_GetSwFuseStatus` | 332 | прочитать статус SW-fuse |
| 0x2438 | `DisplayCore_GetFeatureName` | 228 | имя фичи по ID |
| 0x2524 | `DisplayCore_Open` | 296 | открыть дескриптор DisplayCore |

**Уточнение к части 1:** `DisplayCore_Close` находится на `0x20f8` (а не `0x2174`, как было указано). `DisplayCore_ConfigureSwFuse` — на `0x217c`. Это исправление нумерации, обнаруженное при сверке с дизассемблером.

Модуль полностью реализует жизненный цикл Open -> Configure -> GetStatus -> Close для управления программными fuse-битами дисплея.

---

## 5. GPPARAMS - ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ (0x278c-0x3aa8)

В части 1 эти функции были перечислены списком. Здесь — детальный разбор.

### 5.1. Утилиты-возвраты (return-заглушки)

| Адрес | Имя | Размер | Возврат |
|-------|-----|--------|---------|
| 0x278c | `GPParams_ReturnUnsupported` | 8 | код «не поддерживается» |
| 0x32b4 | `GPParams_SessionListReset` | 8 | сброс списка сессий |
| 0x32dc | `GPParams_ReturnOk` | 8 | код успеха |
| 0x32ec | `GPParams_ReturnErr` | 8 | код ошибки |
| 0x32fc | `GPParams_ReturnErr2` | 8 | альтернативный код ошибки |
| 0x5358 | `GPParams_ReturnTrue` | 8 | true |

Это типичные «константные» функции: `MOV W0, #imm; RET`. Компилятор сгенерировал их для единообразной подстановки кодов возврата вместо inline-констант.

### 5.2. Управление списком сессий (thunks)

| Адрес | Имя | Размер | Роль |
|-------|-----|--------|------|
| 0x2fc4 | `GPParams_SessionListClearThunk` | 12 | очистка списка (обёртка) |
| 0x2fd8 | `GPParams_SessionListClearThunk2` | 12 | вторая обёртка очистки |

### 5.3. Жизненный цикл сессии

| Адрес | Имя | Размер | Роль |
|-------|-----|--------|------|
| 0x2e70 | `GPParams_InitListNode` | 16 | инициализация узла списка |
| 0x3754 | `GPParams_EntryInit` | 56 | инициализация записи |
| 0x3794 | `GPParams_newFromMinkInternal` | 340 | создание из MINK-сообщения |
| 0x38f0 | `GPParams_EntryDtor` | 124 | деструктор записи |
| 0x3974 | `GPParams_SessionCtor` | 172 | конструктор сессии |
| 0x3a28 | `GPParams_SessionDtor` | 120 | деструктор сессии |
| 0x3aa8 | `GPParams_Validate` | 64 | валидация сессии |

**Архитектурный вывод:** GPParams реализует полноценную объектную модель сессий GP. `newFromMinkInternal` (340 б) — точка интеграции с MINK IPC: создаёт запись сессии из входящего MINK-сообщения. Полный цикл: EntryInit -> SessionCtor -> Validate -> EntryDtor -> SessionDtor.

---

## 6. OBJECTLIST - КОНТЕЙНЕР ОБЪЕКТОВ (0x2fec-0x3228)

| Адрес | Имя | Размер | Роль |
|-------|-----|--------|------|
| 0x2fec | `ObjectList_lookup_code` | 44 | поиск кода по объекту |
| 0x3018 | `ObjectList_status_to_code` | 32 | конвертация статуса в GP-код |
| 0x3040 | `ObjectList_get` | 64 | получить элемент |
| 0x3088 | `ObjectList_find` | 92 | найти элемент |
| 0x30ec | `ObjectList_append` | 308 | добавить элемент |
| 0x3228 | `ObjectList_clear` | 132 | очистить список |

**Роль:** классический связный список GP-объектов. `append` (308 б) — самая крупная: реализует вставку с проверкой дубликатов и выделением памяти. `status_to_code` — мост между внутренними статусами и кодами возврата GP TEE (TEE_SUCCESS, TEE_ERROR_*).

---

## 7. LIBC-ФУНКЦИИ (слинкованы статически)

| Адрес | Имя | Размер | Назначение |
|-------|-----|--------|------------|
| 0x26cc | `scalbn` | 148 | масштабирование double по степени 2 |
| 0x3cc4 | `strlcpy` | 112 | безопасная копия строки |
| 0x4068 | `memchr` | 52 | поиск байта в памяти |
| 0x409c | `memset` | 200 (10 xref) | заполнение памяти |
| 0x42d8 | `strlen` | 100 (8 xref) | длина строки |
| 0x433c | `strchr` | 52 | поиск символа в строке |

### 7.1. Разбор `scalbn` @ 0x26cc

Проверено дизассемблером: это полноценная реализация (не заглушка). Алгоритм:
1. `CMP W0, #0x400` + `B.LT` — проверка границ экспоненты
2. Работа с константами `0x7FE0000000000000` и `0xBFD` — манипуляция битами double-формата IEEE-754
3. `FMUL/FMOV` — умножение на степени двойки

Используется совместно с `ldexp` @ 0x85bc для CBOR-кодирования float/double значений.

### 7.2. Разбор `__funcs_on_exit_wrapper` @ 0x27a0 (не libc, но смежная)

Единственная функция в части 1 с 0 xref. Разбор показал:
- `DSB SY` + `ISB` — барьеры памяти (синхронизация перед вызовом деструкторов)
- Загрузка `__imp___funcs_on_exit` из GOT (`off_D110`)
- `CBZ X8, locret` — если указатель NULL, выйти (защита от пустой таблицы)
- `BL __funcs_on_exit` — вызов деструкторов

Это обёртка над `__funcs_on_exit`, вызываемая **через указатель** (поэтому IDA не видит прямых xref). Барьеры памяти гарантируют, что все записи завершены до запуска деструкторов.

---

## 8. COMMONUTILS - КОДИРОВАНИЕ (0x3af0-0x3cb0)

| Адрес | Имя | Размер | Роль |
|-------|-----|--------|------|
| 0x3af0 | `CommonUtils_Base64Encode` | 100 | Base64-кодирование |
| 0x3b54 | `CommonUtils_BytesToHex` | 148 | байты -> hex-строка |
| 0x3be8 | `CommonUtils_HexToBytes` | 200 | hex-строка -> байты |
| 0x3cb0 | `CommonUtils_LoadPair` | 20 | загрузка пары (ключ-значение) |

### 8.1. Разбор `CommonUtils_LoadPair` @ 0x3cb0

Проверено дизассемблером — это 5 инструкций:
```
LDP  X8, X9, [X23]      ; загрузить два 64-битных значения из [X23]
ADD  X8, X8, X22        ; X8 += X22 (смещение)
SUB  X9, X9, X22        ; X9 -= X22
STP  X8, X9, [X23]      ; сохранить обратно
RET
```

**Роль:** атомарная загрузка/модификация пары счётчиков по базовому адресу X23 со смещением X22. Используется для учёта парных величин (например, начало/конец диапазона).

### 8.2. Разбор `CommonUtils_HexToBytes` @ 0x3be8

Проверено дизассемблером — полноценная реализация (200 б). Пролог сохраняет X19-X22 (много регистров), использует `CSEL` для обработки регистра/цифр, `MOV W2, #0xE8`. Это классический парсер hex-строки с валидацией.

---

## 9. ПРОЧЕЕ

| Адрес | Имя | Размер | Примечание |
|-------|-----|--------|------------|
| 0x279c | `j_qsee_err_fatal` | 4 | `B qsee_err_fatal` — трамплин на 0x50 |
| 0x27a0 | `__funcs_on_exit_wrapper` | 40 | обёртка деструкторов (см. 7.2) |
| 0x32c4 | `nullsub_2` | 4 | пустышка (RET) |
| 0x32d0 | `nullsub_3` | 4 | пустышка (RET) |
| 0x4370 | `__stack_chk_guard_ref` | 12 | ссылка на stack canary |

### 9.1. `j_qsee_err_fatal` @ 0x279c

Проверено: это 4 байта — одна инструкция `B qsee_err_fatal` (0x50). «j_» в имени — префикс IDA для функций-переходов (jump thunk). 26 xref — это самая горячая ветвь: все проверки ошибок в TA ведут сюда.

### 9.2. `__stack_chk_guard_ref` @ 0x4370

12 байт. Не функция, а ссылка на глобальную переменную stack canary — используется прологом функций для защиты от переполнения стека. Подтверждает, что TA собрана с `-fstack-protector`.

---

## 10. ИТОГ ПО ЧАСТИ 3

Часть 3 закрыла пробелы обзорного разбора частей 1-2:

1. **Импорт-обёртки (24 шт.)** — разобран единый ARM64 GOT-шаблон `ADRP/LDR/ADD/BR`. Это ключ к пониманию, как TA вызывает внешние API QSEE.
2. **Jump-трамплины (7 шт.)** — 8-байтовые `MOV Xn,X0; BR Xn`, регистровые вентили ABI.
3. **DisplayCore (7 шт.)** — восстановлены реальные размеры; исправлена нумерация (`Close` на 0x20f8, `ConfigureSwFuse` на 0x217c).
4. **GPParams (14 шт.)** — полная объектная модель сессий GP с интеграцией MINK.
5. **ObjectList (6 шт.)** — связный список с конвертацией статусов в GP-коды.
6. **libc (6 шт.)** — `scalbn` подтверждён как полноценная реализация IEEE-754.
7. **CommonUtils (4 шт.)** — разобраны `LoadPair` (5 инструкций) и `HexToBytes`.
8. **Прочее (5 шт.)** — `j_qsee_err_fatal` оказался jump-thunk; `__funcs_on_exit_wrapper` — обёртка с барьерами памяти.

**Общий итог по трём частям:** бинарник `featenabler.img` полностью разобран — 208 функций, ~36.6 КБ кода. Часть 1 — бизнес-логика, часть 2 — инфраструктура, часть 3 — детализация обзорных мест (обёртки, трамплины, мелкие утилиты).

---

**Примечание:** доклад составлен на основе данных текущей IDB `featenabler.img.i64`. Все адреса и размеры сверены с дизассемблером. Изменения не сохранены на диск — при необходимости сохраните базу в IDA (`Ctrl+S`).
